/**
 * Centralized Super Admin logo configuration.
 * The logo is imported from the project's own assets so Vite can
 * hash and optimise it at build time. Change this import to
 * replace the logo everywhere in the Super Admin UI.
 */
import superAdminLogo from '../assets/super-admin-logo.png';

export const SUPER_ADMIN_LOGO = superAdminLogo;